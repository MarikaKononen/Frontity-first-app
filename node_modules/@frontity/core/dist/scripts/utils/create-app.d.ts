import { MultiCompiler } from "webpack";
import express from "express";
import { Mode } from "../../types";
declare const _default: ({ mode, port, isHttps, target }: {
    mode: Mode;
    port: number;
    isHttps: boolean;
    target: "module" | "es5";
}) => Promise<{
    app: express.Express;
    done: (compiler: MultiCompiler) => void;
}>;
export default _default;
