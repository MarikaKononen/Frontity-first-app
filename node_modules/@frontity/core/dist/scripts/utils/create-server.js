"use strict";
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (Object.hasOwnProperty.call(mod, k)) result[k] = mod[k];
    result["default"] = mod;
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
const fs_extra_1 = require("fs-extra");
const http = __importStar(require("http"));
const https = __importStar(require("https"));
// Create HTTP or HTTPS server using a self-signed certificate.
exports.default = async ({ app, isHttps = true }) => {
    if (isHttps) {
        const key = await fs_extra_1.readFile("src/certs/localhost.key");
        const cert = await fs_extra_1.readFile("src/certs/localhost.cert");
        const options = { key, cert };
        return https.createServer(options, app);
    }
    return http.createServer(app);
};
