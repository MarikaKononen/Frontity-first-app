import "./utils/envs";
import { Mode } from "../types";
declare const _default: ({ isHttps, mode, port, target }: {
    port: number;
    isHttps: boolean;
    mode: Mode;
    target: "module" | "es5";
}) => Promise<void>;
export default _default;
